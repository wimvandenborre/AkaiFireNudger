# Archived T-1-inspired velocity groove

Original `VelocityGroove.java`, `AccentHandler.java`, and their checks, copied
before replacing the active page with Groove Shapes. These files are outside
Maven source roots and are not included in the extension. Git history at `beec361`
also retains the original `DrumSequenceMode.applyVelocityGroove` integration.
Normal/accent note-entry velocities remain active; the archived contour does not
modify new or existing notes in the new build.
